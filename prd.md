# PRD — vocab-curator 서비스 (MVP, 제출용)

**서비스명:** vocab-curator  
**공개 URL:** https://foxibu.is-a.dev:5555  
**한 줄 정의:** 내가 고른 콘텐츠에서, 내 상황에 맞는 어휘를 뽑아주는 서비스. 같은 콘텐츠라도 누가 보느냐에 따라 카드가 달라진다.  
**타깃:** 미국 버거집 알바 준비 중인 워홀러 (예시1) — 내일 카운터 손님 응대용 표현을 바로 뽑아야 하는 사람.  
**문제:** 시중 단어장은 평균 학습자 대상 고정 목록이라, 이미 아는 단어를 다시 보거나 내 콘텐츠엔 안 나오는 단어를 외우게 됨. 예문도 무국적. → 내 콘텐츠+내 상황에서 맞춤형으로 필요.  
**트리거:** 출근 전날 밤(원문+내 상황 넣고 결과 받는 때) + 당일 아침(출근 직전 우선순위 높은 것만 다시 보는 때).  
**핵심 기능 (P0, 최대 4개):**
1. 원문 텍스트 + 페르소나(상황) 선택 → vocab-curator식 단어 선별(최대 15개) + 12개 항목 작성
2. 화면용 표·대화 + "오늘의 3단어" (우선순위 최고 3개 + 한 장면 2~3문장 대화)
3. Anki 임포트용 CSV 다운로드 (12개 열, UTF-8, RFC4180)
4. 우선순위 "지금 바로" 위주 첫 화면 + 나머지 접어두기

**P1 (추후):** 목표 언어 선택, 이미 아는 단어 목록, "어렵다/쉬웠다" 체크, 아침 다시 보기(localStorage), 다른 스킬 조합(JLPT 수준 판단 등)

**스킬 매핑:**
- vocab-curator (본인 예선 제출, A-0 안전 점검 통과) — 외부 API·공개 데이터 없이 원문+프로필만으로 동작
- /api(server.js)가 Solar Pro 4 API 직접 호출: base_url=https://api.upstage.ai/v1, model=solar-pro4, OpenAI SDK 호환. 그 외 외부 주소 호출 없음.

**입력:** 원문 텍스트(필수), 내 상황 한 줄~두 줄(있으면 더 맞춤), (P1) 이미 아는 단어 목록, (P1) "어렵다/쉬웠다" 체크  
**출력:** 프로필 해석 3줄(근거 약하면 "불명확"), 화면용 표·대화, "오늘의 3단어", Anki CSV 다운로드, 우선순위 "지금 바로" 위주 첫 화면

**필요 설정:**
- 배포: 공개 URL https://foxibu.is-a.dev:5555 (로그인·회원가입 없이 바로 사용 가능)
- 서비스 키(환경변수): `UPSTAGE_API_KEY` — /api가 Solar Pro 4 API 호출용, 발급처=Upstage Console. 실제 키 값·보유 여부는 사용자 확인. 코드/PRD/UI에 값 노출 금지.
- 사용자 프로필: 내 상황 한 줄~두 줄(핵심). localStorage 저장, 재방문 시 재입력 안 함, 초기화 버튼.

**데이터 규칙:** vocab-curator는 외부 API 없이 원문+프로필만으로 동작. /api만 Solar Pro 4 API 호출(LLM). 없는 값은 '확인 불가'. 추정 금지. 키·프록시 주소는 서버(/api)에만, 클라이언트(index.html)에는 0개. /api가 호출하는 외부 주소 = https://api.upstage.ai/v1 하나.

**성공 기준:**
- 공개 URL(https://foxibu.is-a.dev:5555)에서 누구나 로그인 없이 접근 가능
- 데모: 버거집 워홀러가 전날 밤 원문+내 상황 입력 → 프로필 해석 3줄 + CSV + 오늘의 3단어(for here or to go? / make that a combo? / here is your change) + 대화 → CSV 다운로드 → 아침에 우선순위 높은 3개만 다시 보고 출근
- 시크릿 창 테스트: 저장값·계정 없는 첫 방문 시 안내가 스스로 나오고 최소 기능(입력→결과) 동작

**제외 범위:** 회원가입/계정/로그인, 여러 기기 동기화·누적 히스토리, 발음 오디오, (P1) 목표 언어/이미 아는 단어/어렵다·쉬웠다/아침 다시 보기/현장 만회용/ JLPT 수준 판단, 이미지 입력/OCR

---

**스킬 안전 점검 (vocab-curator):** 통과 — 외부 API·키(환경변수 제외)·외부 전송·권한 요구 없음, 비밀번호·키 입력 요구 없음. /api에서 Solar Pro 4 API 호출 시 키(UPSTAGE_API_KEY)는 서버 환경변수로만 사용, 클라이언트에 노출 없음.
