# PRD — vocab-curator 서비스 (MVP, 제출용)

**서비스명:** vocab-curator  
**공개 URL:** https://foxibu.is-a.dev/vocab/  
**한 줄 정의:** 내가 고른 콘텐츠에서, 내 상황에 맞는 어휘를 뽑아주는 서비스. 같은 콘텐츠라도 누가 보느냐에 따라 카드가 달라진다.  
**타깃:** 학습 목적이 구체적인 사람("일본어 잘하고 싶다"가 아니라 "이 콘텐츠를 자막 없이 보고 싶다" 수준), 실전 지향적인 사람(시험이 아니라 알바·유학·랩실 등 실제 상황에서 곧 써야 하는 사람), 자기가 좋아하는 콘텐츠로 학습하려는 사람. 대표 예시: 미국 버거집 알바 준비 중인 워홀러 — 내일 카운터 손님 응대용 표현을 바로 뽑아야 하는 사람.  
**문제:** 시중 단어장은 평균 학습자 대상 고정 목록이라, 이미 아는 단어를 다시 보거나 내 콘텐츠엔 안 나오는 단어를 외우게 됨. 예문도 무국적. → 내 콘텐츠+내 상황에서 맞춤형으로 필요.  
**트리거:** ① 새 콘텐츠를 접한 직후(예: 알바 전 브이로그 자막을 봤는데 모르는 표현이 많을 때) — 그 자리에서 원문을 붙여넣고 페르소나를 골라 바로 결과를 받는 시점. ② 그 상황을 실제로 맞닥뜨리기 직전(예: 출근 5분 전, 랩 미팅 직전) — 전날 뽑아둔 "지금 바로" 우선순위 표현만 다시 훑어보는 시점.  
**핵심 기능 (P0):**
1. 원문 텍스트 입력(붙여넣기 또는 .txt/.pdf 업로드) + 페르소나 선택(최초 방문 시 예시 페르소나 자동 제공, 추가/수정/삭제 가능) → vocab-curator식 단어 선별(최대 20개, 기본 15개) + 12개 항목 작성
2. 화면용 표·단어 카드 + "오늘의 3단어" (우선순위 최고 3개 + 한 장면 2~3문장 대화)
3. Anki 임포트용 CSV 다운로드 (12개 열, UTF-8, RFC4180)
4. 우선순위 "지금 바로" 위주 첫 화면 + 나머지는 우선도에 따라 표시/접기
5. 페르소나별 피드백 저장 — 예문 스타일·난이도·원하는 표현 등을 페르소나에 메모해두면, 다음 추출부터 그 피드백이 생성 프롬프트에 최우선으로 반영되어 결과가 그 사람 취향에 맞게 조정됨

**P1 (추후):** 목표 언어 선택, 이미 아는 단어 목록, "어렵다/쉬웠다" 체크, 아침 다시 보기(localStorage), 오늘의 복습 페이지(오늘 뽑은 표현 상위 항목 다시 보기 + 저녁 리마인드 알림), 다른 스킬 조합(JLPT 수준 판단 등)

**스킬 매핑:**
- vocab-curator (본인 예선 제출, A-0 안전 점검 통과) — 외부 API·공개 데이터 없이 원문+프로필만으로 동작
- /api(server.js)가 Solar Pro 4 API 직접 호출: base_url=https://api.upstage.ai/v1, model=solar-pro4, OpenAI SDK 호환. 그 외 외부 주소 호출 없음.

**입력:**
- **필수:** 원문 텍스트(붙여넣기 또는 .txt/.pdf 업로드), 페르소나 선택(핵심 — 상황 텍스트가 곧 프로필)
- **선택(도입됨):** 페르소나 추가/수정/삭제, 이미 아는 단어 목록, 추출 개수, 집중 단어, 페르소나 피드백(예문 스타일·난이도·원하는 수준 등), 원문 언어/맥락 정보, 상황/타겟 언어 맥락 정보
- (P1) "어렵다/쉬웠다" 체크, 아침 다시 보기(localStorage)

**출력:** 프로필 해석 3줄(근거 약하면 "불명확"), 화면용 표·단어 카드, "오늘의 3단어", Anki CSV 다운로드, 우선순위 "지금 바로" 위주 첫 화면

**필요 설정:**
- 배포: 공개 URL https://foxibu.is-a.dev/vocab/ (로그인·회원가입 없이 바로 사용 가능)
- 서비스 키(환경변수): `UPSTAGE_API_KEY` — /api가 Solar Pro 4 API 호출용, 발급처=Upstage Console. 실제 키 값·보유 여부는 사용자 확인. 코드/PRD/UI에 값 노출 금지.
- 사용자 프로필: 페르소나(이름/한 줄 설명/상황) 단위로 localStorage 저장. 최초 방문 시 예시 페르소나 자동 생성·선택, 재방문 시 재입력 안 함, 페르소나별 추가/수정/삭제 가능.

**데이터 규칙:** vocab-curator는 외부 API 없이 원문+프로필만으로 동작. /api만 Solar Pro 4 API 호출(LLM). 없는 값은 '확인 불가'. 추정 금지. 키·프록시 주소는 서버(/api)에만, 클라이언트(index.html)에는 0개. /api가 호출하는 외부 주소 = https://api.upstage.ai/v1 하나.

**성공 기준:**
- 공개 URL(https://foxibu.is-a.dev/vocab/)에서 누구나 로그인 없이 접근 가능
- 데모: 버거집 워홀러가 전날 밤 원문 입력 + 페르소나 선택 → 프로필 해석 3줄 + CSV + 오늘의 3단어(그 회차 우선순위 상위 표현 + 장면 대화) → CSV 다운로드 → 아침에 우선순위 높은 3개만 다시 보고 출근
- 시크릿 창 테스트: 저장값·계정 없는 첫 방문 시 안내가 스스로 나오고 최소 기능(입력→결과) 동작

※ "아침에 우선순위 높은 3개만 다시 보는" 흐름은 P1(오늘의 복습 페이지)에 해당한다.

**제외 범위:** 회원가입/계정/로그인, 여러 기기 동기화·누적 히스토리, 발음 오디오, 목표 언어 선택, "어렵다/쉬웠다" 체크, 현장 만회용, JLPT 수준 판단, 이미지 입력/OCR

※ 오늘의 복습 페이지(review.html)는 P1에 해당하므로, 현재 상태에서는 본 서비스(index.html)의 필수 흐름이 아니다.

---

**스킬 안전 점검 (vocab-curator):** 통과 — 외부 API·키(환경변수 제외)·외부 전송·권한 요구 없음, 비밀번호·키 입력 요구 없음. /api에서 Solar Pro 4 API 호출 시 키(UPSTAGE_API_KEY)는 서버 환경변수로만 사용, 클라이언트에 노출 없음.
